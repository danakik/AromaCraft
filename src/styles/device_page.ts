import Stylesheet from 'reactjs-stylesheet';

const styles = Stylesheet.create({
  pid: {
    position: 'absolute',
    top: 500,
    left: 350,
  },

  pid_temp: {
    position: 'absolute',
    top: 520,
    left: 355,
  },

  pid_percent: {
    position: 'absolute',
    top: 535,
    left: 355,
  },

  
});
export default styles;
